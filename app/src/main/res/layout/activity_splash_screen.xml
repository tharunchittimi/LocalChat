<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="splashScreen"
            type="com.example.localchat.ui.splashscreen.SplashScreenViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/snack_color"
        tools:context="com.ci.basearchitecture.ui.splashscreen.SplashScreenActivity">

        <ImageView
            android:id="@+id/imgLoginLogo"
            android:layout_width="@dimen/dp100"
            android:layout_height="@dimen/dp100"
            android:src="@drawable/ic_logo"
            android:contentDescription="@null"
            app:layout_constraintBottom_toTopOf="@id/txtOralTechHeading"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintVertical_chainStyle="packed"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


        <com.example.localchat.widgets.CustomTextView
            android:id="@+id/txtOralTechHeading"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:textSize="@dimen/sp24"
            android:gravity="center"
            android:layout_margin="@dimen/_10dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/imgLoginLogo"
            app:layout_constraintBottom_toBottomOf="parent"
            app:setFont="HelveticaNeue-Bold"
            android:text="LOCAL CHAT" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>

